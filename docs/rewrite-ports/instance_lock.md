# instance_lock
旧テスト 3 本 → 新テスト 4 本

| 旧テスト名 | 扱い | 新テスト名 / 削除理由 |
|---|---|---|
| 先に取ったロックを離すまで次の取得は拒まれる | 移植 | 同名で移植 |
| 別のリポジトリ同士は邪魔し合わない | 移植 | 同名で移植 |
| ロックはexecを越えて残らない | 移植 | 同名で移植 |
| (新規) | 追加 | 全worktreeでロックは1つ — worktree からのロックがメインリポジトリと同じ対象を掴み、
どちらから取ってももう一方を排他する不変条件をリンク worktree との往復で固定 (旧テストには無かった) |

API 変更: なし。`InstanceLock` / `lock_path()` / `locked_repo_root()` / `acquire()` を
そのまま移植。

残したコメント (なぜ): ロック失敗を警告止まりにする理由 (起動をブロックしない設計判断)。
`acquire()` の `unsafe` ブロックの SAFETY コメント (fd が直前に開いたものであることの保証)。
